{
    "files.exclude": {
        "**/node_modules/": true,
        "**/*.js": {"when": "$(basename).ts"},
        "**/*.js.map": {"when": "$(basename)"}
    }
,
"typescript.check.workspaceVersion": false
}